import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const Sport = () => {
  return (
    <View>
      <Text>Sport</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
