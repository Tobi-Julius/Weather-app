import { View, Text } from "react-native";
import React from "react";

export const ForecastBody = ({ data }) => {
  console.warn(data);
  return (
    <View>
      <Text>ForecastBody</Text>
    </View>
  );
};
